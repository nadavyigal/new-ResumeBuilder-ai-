# CURSOR.md — ResumeBuilder Web

> Cursor-native router. `AGENTS.md` has repo guidelines; `CLAUDE.md` has session rules.

## Session Start

1. Read `~/.claude/MEMORY.md` and `~/.claude/ERRORS.md`
2. Read `tasks/MEMORY.md` and `tasks/ERRORS.md`
3. Read `tasks/lessons.md`
4. State the objective in one sentence

## Project Notes

- Stack: Next.js, TypeScript, Supabase, OpenAI, Tailwind CSS
- Done gate: `npm run lint && tsc --noEmit && npm run build`
- Planning via Speckit (`.specify/`, `.cursor/commands/speckit.*`)
- Use plan mode for multi-file structural changes

## What NOT to Import

- Do not copy `.cursor/skills/` from RunSmart Web
- Do not duplicate `AGENTS.md` content into `.cursor/rules/`

## Cross-Project Context

- Global Agentic OS: `/Users/nadavyigal/Documents/Projects /Agentic OS/AGENTS.md`
- Load `PROJECT-BRIDGES/resumebuilder-ai.md` only for cross-project work
